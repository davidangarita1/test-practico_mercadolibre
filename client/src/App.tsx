import './App.scss'
import React, {ReactElement} from 'react';
import Router from "./Router";

const App = (): ReactElement => {
    return (
        <>
            <Router/>
        </>

    );
}

export default App;
